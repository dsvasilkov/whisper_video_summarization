# Worker для очереди llm.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: whisper-worker-llm
  namespace: whisper
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: whisper-worker-llm
  template:
    metadata:
      labels:
        app: whisper-worker-llm
    spec:
      containers:
        - name: worker-llm
          image: whisper-worker:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: metrics
              containerPort: 9101
          startupProbe:
            exec:
              command:
                - /bin/sh
                - -lc
                - exec curl -fsS "${VLLM_LLM_BASE_URL}/models" >/dev/null
            periodSeconds: 2
            timeoutSeconds: 5
            failureThreshold: 30
          volumeMounts:
            - name: uploads
              mountPath: /app/data
          command:
            - aio_celery
            - worker
            - whisper_video_summarization.celery_app.app:celery_app
            - -Q
            - llm
            - -l
            - INFO
            - --concurrency=1
          resources:
            requests:
              memory: "4Gi"
              cpu: "2"
            limits:
              memory: "6Gi"
              cpu: "4"
          env:
            - name: PYTHONPATH
              value: /app
            - name: DATABASE_URL
              value: postgresql+psycopg2://postgres:postgres@postgres.whisper.svc.cluster.local:5432/whisper_inference
            - name: CELERY_BROKER_URL
              value: amqp://guest:guest@rabbitmq.whisper.svc.cluster.local:5672//
            - name: CELERY_RESULT_BACKEND
              value: redis://redis.whisper.svc.cluster.local:6379/0
            - name: VLLM_LLM_BASE_URL
              value: http://vllm-llm.whisper.svc.cluster.local:8000/v1
            # unit_graph эмбеддит тысячи фрагментов до первого вызова vLLM; без URL грузится BGE в процесс воркера (очень долго).
            - name: RAG_EMBEDDINGS_SERVE_URL
              value: http://ray-serve.whisper.svc.cluster.local:8000/embeddings
            - name: QDRANT_URL
              value: http://qdrant.whisper.svc.cluster.local:6333
            - name: HF_HOME
              value: /app/data/hf
            - name: WORKER_METRICS_PORT
              value: "9101"
            - name: PROMETHEUS_MULTIPROC_DIR
              value: /tmp/prometheus_multiproc
      volumes:
        - name: uploads
          persistentVolumeClaim:
            claimName: whisper-uploads

---
apiVersion: v1
kind: Service
metadata:
  name: whisper-worker-llm-metrics
  namespace: whisper
spec:
  selector:
    app: whisper-worker-llm
  ports:
    - name: metrics
      port: 9101
      targetPort: 9101
