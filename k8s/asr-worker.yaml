apiVersion: apps/v1
kind: Deployment
metadata:
  name: whisper-worker-asr
  namespace: whisper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: whisper-worker-asr
  template:
    metadata:
      labels:
        app: whisper-worker-asr
    spec:
      containers:
        - name: worker-asr
          image: whisper-worker-asr:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: metrics
              containerPort: 9101
          volumeMounts:
            - name: uploads
              mountPath: /app/data
          command:
            - aio_celery
            - worker
            - whisper_video_summarization.celery_app.app:celery_app
            - -Q
            - asr
            - -l
            - INFO
            - --concurrency=1
          resources:
            requests:
              memory: "4Gi"
              cpu: "2"
            limits:
              memory: "6Gi"
              cpu: "4"
          env:
            - name: PYTHONPATH
              value: /app
            - name: DATABASE_URL
              value: postgresql+psycopg2://postgres:postgres@postgres.whisper.svc.cluster.local:5432/whisper_inference
            - name: CELERY_BROKER_URL
              value: amqp://guest:guest@rabbitmq.whisper.svc.cluster.local:5672//
            - name: VLLM_ASR_BASE_URL
              value: http://vllm-asr.whisper.svc.cluster.local:8000/v1
            - name: WORKER_METRICS_PORT
              value: "9101"
            - name: PROMETHEUS_MULTIPROC_DIR
              value: /tmp/prometheus_multiproc
      volumes:
        - name: uploads
          persistentVolumeClaim:
            claimName: whisper-uploads
---
apiVersion: v1
kind: Service
metadata:
  name: whisper-worker-asr-metrics
  namespace: whisper
spec:
  selector:
    app: whisper-worker-asr
  ports:
    - name: metrics
      port: 9101
      targetPort: 9101
