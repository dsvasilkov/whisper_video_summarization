# Локальная разработка: фронт (npm run dev) на :5173, API — на :8000.
# Укажи полный URL с путём /api. Если не задано — используется относительный /api (одна точка входа).
# При API в minikube: kubectl port-forward -n whisper svc/whisper-api 8000:8000
VITE_API_URL=http://localhost:8000/api
